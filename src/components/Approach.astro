---
const differentiators = [
  {
    title: "Not therapy",
    description: "I won't ask you to explore your childhood or sit with your feelings for an hour. This is practical, strategic coaching designed for how your mind works.",
  },
  {
    title: "Not productivity coaching",
    description: "I'm not here to help you squeeze more out of yourself. We're redesigning the system so it actually sustains you.",
  },
  {
    title: "Not wellness fluff",
    description: "No mantras. No vision boards. Evidence-based strategies from neuroscience, behavioral psychology, and leadership research.",
  },
];
---

<section id="approach" class="approach-section">
  <div class="container">
    <div class="approach-grid">
      <div class="approach-intro" data-reveal>
        <span class="section-subtitle">The Approach</span>
        <h2 class="section-title balance">
          Recovery that respects your reality
        </h2>
        <p class="approach-lead">
          You don't have time for solutions that require you to become a different person.
          You need strategies that work within your actual life — the one with back-to-back
          meetings, high-stakes decisions, and people depending on you.
        </p>
        <p class="approach-text">
          My approach is built on a simple premise: <strong>burnout isn't a personal failing.
          It's a systems problem.</strong> Together, we identify the patterns that are depleting
          you and rebuild them — not by doing less, but by leading differently.
        </p>
      </div>

      <div class="approach-differentiators">
        {differentiators.map((item, index) => (
          <div
            class="differentiator-card"
            data-reveal
            style={`transition-delay: ${index * 100}ms`}
          >
            <h3 class="differentiator-title">{item.title}</h3>
            <p class="differentiator-text">{item.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .approach-section {
    padding: var(--space-3xl) 0;
    background: var(--bg-primary);
  }

  .approach-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2xl);
    align-items: start;
  }

  .approach-intro {
    position: sticky;
    top: var(--space-2xl);
  }

  .section-subtitle {
    display: block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--accent);
    margin-bottom: var(--space-sm);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: var(--weight-normal);
    color: var(--text-primary);
    margin-bottom: var(--space-lg);
  }

  .approach-lead {
    font-size: var(--text-lg);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-md);
  }

  .approach-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .approach-text strong {
    color: var(--text-primary);
  }

  .approach-differentiators {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .differentiator-card {
    padding: var(--space-lg);
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    transition: all var(--duration-normal) var(--ease-calm);
  }

  .differentiator-card:hover {
    border-color: var(--accent-muted);
    transform: translateX(4px);
  }

  .differentiator-title {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .differentiator-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    max-width: none;
  }

  @media (max-width: 1024px) {
    .approach-grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }

    .approach-intro {
      position: static;
    }
  }
</style>
