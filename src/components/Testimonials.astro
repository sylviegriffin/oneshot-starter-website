---
const testimonials = [
  {
    quote: "I came in expecting another productivity system. What I got was permission to be human while still being effective. Six months later, I'm performing better than ever — and I'm actually sleeping.",
    attribution: "VP of Engineering, Tech Company",
    highlight: "permission to be human",
  },
  {
    quote: "The discretion was critical for me. No one at work knows I'm doing this. But they've noticed I'm calmer, clearer, and making better decisions.",
    attribution: "Managing Director, Financial Services",
    highlight: "calmer, clearer",
  },
  {
    quote: "I thought burnout meant I needed to quit and move to a beach. Instead, I learned I could recover while staying in my role. The changes were small but the impact was profound.",
    attribution: "Chief Marketing Officer, Healthcare",
    highlight: "recover while staying",
  },
];
---

<section id="testimonials" class="testimonials-section">
  <div class="container">
    <header class="testimonials-header" data-reveal>
      <span class="section-subtitle">Results</span>
      <h2 class="section-title balance">
        What clients say
      </h2>
      <p class="testimonials-note">
        All testimonials are from verified clients. Names and companies withheld for privacy.
      </p>
    </header>

    <div class="testimonials-grid">
      {testimonials.map((testimonial, index) => (
        <blockquote
          class="testimonial-card"
          data-reveal
          style={`transition-delay: ${index * 100}ms`}
        >
          <div class="testimonial-quote">
            <svg class="quote-mark" width="32" height="24" viewBox="0 0 32 24" fill="none" aria-hidden="true">
              <path d="M0 24V14.4C0 10.4 0.8 7.2 2.4 4.8C4 2.4 6.8 0.533333 10.8 -0.266667L12 3.6C9.6 4.26667 7.93333 5.26667 7 6.6C6.06667 7.93333 5.6 9.46667 5.6 11.2H12V24H0ZM20 24V14.4C20 10.4 20.8 7.2 22.4 4.8C24 2.4 26.8 0.533333 30.8 -0.266667L32 3.6C29.6 4.26667 27.9333 5.26667 27 6.6C26.0667 7.93333 25.6 9.46667 25.6 11.2H32V24H20Z" fill="var(--accent-muted)"/>
            </svg>
            <p class="quote-text">
              {testimonial.quote}
            </p>
          </div>
          <footer class="testimonial-footer">
            <cite class="testimonial-attribution">
              {testimonial.attribution}
            </cite>
          </footer>
        </blockquote>
      ))}
    </div>
  </div>

  <!-- Breathing line divider -->
  <div class="section-divider">
    <div class="divider-breathing"></div>
  </div>
</section>

<style>
  .testimonials-section {
    padding: var(--space-3xl) 0 var(--space-2xl);
    background: var(--bg-secondary);
  }

  .testimonials-header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-2xl);
  }

  .section-subtitle {
    display: block;
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-widest);
    color: var(--accent);
    margin-bottom: var(--space-sm);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: var(--text-4xl);
    font-weight: var(--weight-normal);
    color: var(--text-primary);
    margin-bottom: var(--space-md);
  }

  .testimonials-note {
    font-size: var(--text-sm);
    color: var(--text-muted);
    font-style: italic;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .testimonial-card {
    display: flex;
    flex-direction: column;
    padding: var(--space-lg);
    background: var(--bg-primary);
    border-radius: var(--radius-xl);
    border: 1px solid var(--border);
    transition: all var(--duration-normal) var(--ease-calm);
  }

  .testimonial-card:hover {
    border-color: var(--border-hover);
    box-shadow: var(--shadow-md);
  }

  .testimonial-quote {
    flex: 1;
    position: relative;
  }

  .quote-mark {
    margin-bottom: var(--space-md);
  }

  .quote-text {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-style: italic;
    font-weight: var(--weight-normal);
    line-height: var(--leading-relaxed);
    color: var(--text-primary);
    max-width: none;
  }

  .testimonial-footer {
    margin-top: var(--space-lg);
    padding-top: var(--space-md);
    border-top: 1px solid var(--border);
  }

  .testimonial-attribution {
    font-size: var(--text-sm);
    font-style: normal;
    color: var(--text-muted);
  }

  .section-divider {
    padding-top: var(--space-2xl);
  }

  @media (max-width: 1024px) {
    .testimonials-grid {
      grid-template-columns: 1fr;
      max-width: 600px;
      margin: 0 auto;
    }
  }
</style>
